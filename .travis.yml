language: cpp

compiler:
  - gcc
  - clang
    
install:
  - sudo add-apt-repository ppa:smspillaz/cmake-2.8.12 -y
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -qq
  - sudo apt-get purge cmake -qq
  - sudo apt-get install cmake -qq
  - if [ "$CXX" = "clang++" ]; then sudo apt-get install -qq libstdc++-4.8-dev; fi
  - if [ "$CXX" = "g++" ]; then sudo apt-get install -qq g++-4.8; fi
  - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi

before_script:
  - git clone https://github.com/glfw/glfw
  - mkdir glfw/build
  - cd glfw/build
  - cmake .. -DBUILD_SHARED_LIBS=ON
  - make VERBOSE=1
  - sudo make install
  - cd ../..
  - mkdir build
  - cd build
  
script:
  - cmake ..
  - make VERBOSE=1
  - sudo make install

env:
  global:
   # The next declaration is the encrypted COVERITY_SCAN_TOKEN, created
   #   via the "travis encrypt" command using the project repo's public key
   - secure: "g+kbqutuchcAb6+nrW2fJO0whDt1GYLrD5UmmCpK6fasI+3J82VEiywRWYCdifvrASp/XQv0aohdGcOZPwdP/3ii3MY876F626mNaMzAqV2lw6Lo0t8+Gpc1M3/WA/qUm1npjb1A+HzVOG5tQhQyiENjNKGURfh9EmghhWEjenQ="

addons:
  coverity_scan:
    project:
      name: "alexeyknyshev/kiui"
      description: "Build submitted via Travis CI"
    notification_email: alexey.knyshev@gmail.com
    build_command_prepend: "make clean; cmake .."
    build_command:   "make -j4"
    branch_pattern: coverty_scan

os:
  - linux
